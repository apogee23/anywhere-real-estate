import 'package:flutter/material.dart';

class CustomColors {
  /// The background color of a Robinhood crypto coin screen.
  // static const Color screenBackground = Color(0xFF120821);
  static const Color screenBackground = Color(0xFF000000);

  static const Color textColor = Color(0xFFFFFFFF);

  /// The accent color used throughout the crypto UI when the
  /// coin price goes up.
  static const Color primary = Colors.blue;

  /// The accent color used throughout the crypto UI when the
  /// coin price goes down.
  static const Color secondary = Colors.lightBlue;
}
